[project]
name = "portfolio-visualizer"
version = "1.0.0"
dependencies = []
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
